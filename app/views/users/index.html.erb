<div class="body">
    <div class="container">

        <p id="notice"></p>


     <div class="row">

<%= render 'books/link', user: current_user, book: @book %>

  <div class="col-xs-9">
		<h2>Users</h2>
		<table class="table">
		<tbody>
        <tr>
        <th>
          <p>image</p>
        </th>
        <th>
          <p>name</p>
        </th>
        <th>
        </th>
        </tr>
            <% @users.each do |user| %>
		  	<tr>
          <td>
        		<%= attachment_image_tag user, :profile_image, :fill, 40, 40, class: "img-circle pull-left profile-thumb", fallback: "no_image.jpg",:size => '40x40' %>
				  </td>
        		<td><%= user.name %></td>
            <td> <%= link_to 'Show', user_path(user) %></td>
      		</tr>
      		<% end %>
   		</tbody>
   		</table>
      </div>
     </div>
    </div>
</div>
